# About Unsupervised Learning

Unsupervised Learning is a type of machine learning where the model learns patterns from unlabeled data, meaning there are no predefined outputs or target values. The goal is to find hidden structures, relationships, or groupings within the data.

## 👾 Key Characteristics

1. No labeled data – The dataset only contains input features without target labels.
2. Pattern discovery – Focuses on identifying similarities, clusters, or associations among data points.
3. Dimensionality reduction – Simplifies complex datasets while retaining essential information.
4. Useful for – Exploratory data analysis, anomaly detection, customer segmentation, and data compression.

## 👾 Common Algorithms

1. Clustering – K-Means, Hierarchical Clustering, DBSCAN
2. Dimensionality Reduction – PCA (Principal Component Analysis), t-SNE, Autoencoders
3. Association Rule Learning – Apriori, FP-Growth

## 👾 Workflow

  ┌────────────────────┐
  │ 1. Collect &       │
  │    Preprocess Data │
  └─────────┬──────────┘
            │
            ▼
  ┌────────────────────┐
  │ 2. Choose an       │
  │    Algorithm       │
  │  (e.g., K-Means,   │
  │   PCA, DBSCAN)     │
  └─────────┬──────────┘
            │
            ▼
  ┌────────────────────┐
  │ 3. Train / Fit     │
  │    the Model       │
  └─────────┬──────────┘
            │
            ▼
  ┌────────────────────┐
  │ 4. Analyze Results │
  │  (Clusters,        │
  │   Components)      │
  └─────────┬──────────┘
            │
            ▼
  ┌────────────────────┐
  │ 5. Visualize &     │
  │    Interpret Data  │
  └────────────────────┘

  ## 👾 NOTE FOR CONTRIBUTION:

  There are two jupyter notebooks for k-Means and DBSCAN respectively, each of which contains comments about what you need to do. 
  Feel free to contribute, keeping in mind the guidelines. 
  
